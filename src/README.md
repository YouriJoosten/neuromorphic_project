# COMMANDS

Run experiment with gui: 
```bash
python src/experiments/hebbian_single_intersection.py -gui
```

Plot results (paste in whole file name after outputs):
```bash
python outputs/plot.py -f outputs\single-intersection\0_conn0_ep1.csv
```
